---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-16.04
    driver:
      box: 'ubuntu/xenial64'

suites:
  - name: default
    run_list:
      - recipe[get-native.com-cookbook::default]
    attributes:
  - name: web
    run_list:
      - recipe[get-native.com-cookbook::web-server]
    attributes:
  - name: db
    run_list:
      - recipe[get-native.com-cookbook::db-server]
    attributes:
